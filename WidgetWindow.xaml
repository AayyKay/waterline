<Window x:Class="Waterline.WidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Waterline widget" Width="410" Height="490" MinWidth="360" MinHeight="430"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="Manual" MouseLeftButtonDown="Window_MouseLeftButtonDown">
  <Grid>
    <Border x:Name="ExpandedCard" Margin="8" Style="{StaticResource Card}" Background="#F20A1938" CornerRadius="25">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid>
          <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
          <StackPanel Orientation="Horizontal">
            <Image Source="pack://application:,,,/Assets/waterline-app.png" Width="34" Height="34" Margin="0,0,9,0" />
            <TextBlock Text="waterline" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" />
          </StackPanel>
          <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Button Content="Collapse" Click="Collapse_Click" Padding="9,6" Margin="0,0,6,0" />
            <Button Content="×" Click="Close_Click" Padding="10,6" />
          </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Margin="0,14,0,10">
          <Grid.ColumnDefinitions><ColumnDefinition Width="150" /><ColumnDefinition /></Grid.ColumnDefinitions>
          <Grid Width="140" Height="140">
            <Ellipse Stroke="#343E6098" StrokeThickness="12" />
            <Ellipse Stroke="{StaticResource CyanBrush}" StrokeThickness="12" />
            <TextBlock Text="{Binding ProgressPercent, StringFormat={}{0:0}%}" FontSize="27" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Grid>
          <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="14,0,0,0">
            <TextBlock Text="TODAY" Foreground="{StaticResource CyanBrush}" FontSize="10" />
            <TextBlock Text="{Binding ProgressLabel}" FontSize="22" FontWeight="Bold" Margin="0,6,0,3" />
            <TextBlock Text="{Binding RemainingLabel}" Foreground="{StaticResource MutedBrush}" />
          </StackPanel>
        </Grid>
        <Button Grid.Row="2" Style="{StaticResource PrimaryButton}" Content="＋  Log 12 oz" Click="Add12_Click" />
        <UniformGrid Grid.Row="3" Columns="3" Margin="0,9,0,0">
          <Button Content="+ 8 oz" Click="Add8_Click" Margin="0,0,5,0" />
          <Button Content="+ 16 oz" Click="Add16_Click" Margin="5,0" />
          <Button Content="Custom" Click="Custom_Click" Margin="5,0,0,0" />
        </UniformGrid>
        <Border Grid.Row="4" Background="#5511264D" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="14" Padding="13" Margin="0,14,0,0">
          <StackPanel>
            <TextBlock Text="{Binding ReminderLabel}" Foreground="{StaticResource MutedBrush}" FontSize="11" />
            <TextBlock Text="{Binding PaceTitle}" FontWeight="SemiBold" Margin="0,5,0,0" />
          </StackPanel>
        </Border>
      </Grid>
    </Border>

    <Border x:Name="CollapsedOrb" Width="104" Height="104" CornerRadius="52" Visibility="Collapsed"
            Background="#FA102B59" BorderBrush="{StaticResource CyanBrush}" BorderThickness="2">
      <Grid>
        <Image Source="pack://application:,,,/Assets/waterline-app.png" Width="57" Height="57" />
        <TextBlock Text="{Binding ProgressPercent, StringFormat={}{0:0}%}" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,7" />
        <Button Content="⌄" Click="Expand_Click" Width="32" Height="25" Padding="0" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,-12" />
      </Grid>
    </Border>
  </Grid>
</Window>
