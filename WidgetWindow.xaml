<Window x:Class="Waterline.WidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Waterline"
        xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationCore"
        Title="Waterline widget" Width="400" Height="500"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Icon="pack://application:,,,/Assets/waterline-app.ico"
        WindowStartupLocation="Manual" PreviewMouseLeftButtonDown="Window_PreviewMouseLeftButtonDown">
  <Window.Resources>
    <Storyboard x:Key="WidgetEntrance">
      <DoubleAnimation Storyboard.TargetName="WidgetSurface" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:.2" />
      <DoubleAnimation Storyboard.TargetName="WidgetTranslate" Storyboard.TargetProperty="Y" From="8" To="0" Duration="0:0:.24" />
    </Storyboard>
    <Style x:Key="WidgetCaptionButton" TargetType="Button" BasedOn="{StaticResource IconButton}">
      <Setter Property="Width" Value="36" /><Setter Property="Height" Value="36" /><Setter Property="Padding" Value="8" />
    </Style>
  </Window.Resources>

  <Grid x:Name="WidgetSurface" Opacity="0">
    <Grid.RenderTransform><TranslateTransform x:Name="WidgetTranslate" /></Grid.RenderTransform>
    <Border x:Name="ExpandedCard" Margin="8" CornerRadius="20" BorderBrush="{DynamicResource BoundaryBrush}" BorderThickness="1" Background="{DynamicResource CanvasRaisedBrush}">
      <Border.Effect><DropShadowEffect Color="#B0000618" BlurRadius="34" ShadowDepth="10" Opacity=".7" /></Border.Effect>
      <Grid ClipToBounds="True">
        <Ellipse Width="460" Height="190" Fill="{DynamicResource AtmosphereBrush}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-170,-95,0,0" IsHitTestVisible="False" />
        <Grid Margin="20">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <Viewbox Width="28" Height="28"><Path Data="{StaticResource IconWaterline}" Stroke="{DynamicResource CyanBrush}" StrokeThickness="1.6" /></Viewbox>
              <StackPanel Margin="9,0,0,0"><TextBlock Text="waterline" FontSize="17" FontWeight="SemiBold" /><TextBlock Text="DESKTOP COMPANION" Style="{StaticResource MetadataText}" FontSize="9" /></StackPanel>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
              <Button Style="{StaticResource WidgetCaptionButton}" Click="Collapse_Click" ToolTip="Compact widget" automation:AutomationProperties.Name="Compact widget"><Path Data="M4,7 L10,13 L16,7" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.6" Width="16" Height="16" Stretch="Uniform" /></Button>
              <Button Style="{StaticResource WidgetCaptionButton}" Click="Close_Click" ToolTip="Close widget" automation:AutomationProperties.Name="Close widget" Margin="6,0,0,0"><Path Data="{StaticResource IconClose}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.6" Width="16" Height="16" Stretch="Uniform" /></Button>
            </StackPanel>
          </Grid>

          <Grid Grid.Row="1" Margin="0,10,0,8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="154" /><ColumnDefinition /></Grid.ColumnDefinitions>
            <Grid Width="142" Height="142">
              <local:ProgressRing Value="{Binding ProgressPercent}" StrokeThickness="7" automation:AutomationProperties.Name="Today’s hydration progress" />
              <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding ProgressPercent, StringFormat={}{0:0}%}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" />
                <TextBlock Text="TODAY" Style="{StaticResource MetadataText}" Foreground="{DynamicResource CyanBrush}" HorizontalAlignment="Center" />
              </StackPanel>
            </Grid>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
              <TextBlock Text="CURRENT PROGRESS" Style="{StaticResource MetadataText}" Foreground="{DynamicResource AquaBrush}" />
              <TextBlock Text="{Binding ProgressLabel}" FontSize="23" FontWeight="SemiBold" Margin="0,7,0,0" />
              <TextBlock Text="{Binding RemainingLabel}" Style="{StaticResource SecondaryText}" FontSize="13" Margin="0,4,0,0" TextWrapping="Wrap" />
            </StackPanel>
          </Grid>

          <Button Grid.Row="2" Style="{StaticResource PrimaryButton}" Content="Log 12 oz" Click="Add12_Click" automation:AutomationProperties.Name="Log 12 ounces from widget" />
          <UniformGrid Grid.Row="3" Columns="3" Margin="0,9,0,0">
            <Button Content="+ 8 oz" Click="Add8_Click" Margin="0,0,5,0" automation:AutomationProperties.Name="Log 8 ounces" />
            <Button Content="+ 16 oz" Click="Add16_Click" Margin="4,0" automation:AutomationProperties.Name="Log 16 ounces" />
            <Button Content="Custom" Click="Custom_Click" Margin="5,0,0,0" automation:AutomationProperties.Name="Log a custom amount" />
          </UniformGrid>

          <Border Grid.Row="4" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource DividerBrush}" BorderThickness="1" CornerRadius="12" Padding="14" Margin="0,13,0,0">
            <Grid>
              <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
              <Grid><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="{Binding ReminderLabel}" Style="{StaticResource SecondaryText}" /><TextBlock Grid.Column="1" Text="Always on top" Style="{StaticResource MetadataText}" /></Grid>
              <Grid Grid.Row="1" Margin="0,7,0,0"><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="{Binding PaceTitle}" FontWeight="SemiBold" /><Button Grid.Column="1" Style="{StaticResource QuietButton}" Content="Open Waterline" Click="OpenDashboard_Click" Padding="8,4" automation:AutomationProperties.Name="Open Waterline dashboard" /></Grid>
            </Grid>
          </Border>
        </Grid>
      </Grid>
    </Border>

    <Border x:Name="CompactCard" Width="104" Height="168" CornerRadius="18" HorizontalAlignment="Center" VerticalAlignment="Center" Background="{DynamicResource CanvasRaisedBrush}" BorderBrush="{DynamicResource CyanBrush}" BorderThickness="1.5" Visibility="Collapsed">
      <Border.Effect><DropShadowEffect Color="#B0000618" BlurRadius="28" ShadowDepth="8" Opacity=".7" /></Border.Effect>
      <Grid Margin="8">
        <Grid.RowDefinitions><RowDefinition Height="30" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
        <Viewbox Width="22" Height="22" HorizontalAlignment="Left" VerticalAlignment="Center"><Path Data="{StaticResource IconWaterline}" Stroke="{DynamicResource CyanBrush}" StrokeThickness="1.7" /></Viewbox>
        <Grid Grid.Row="1" Width="78" Height="78">
          <local:ProgressRing Value="{Binding ProgressPercent}" StrokeThickness="4" automation:AutomationProperties.Name="Today’s hydration progress" />
          <TextBlock Text="{Binding ProgressPercent, StringFormat={}{0:0}%}" FontSize="17" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
        <Button Grid.Row="2" Style="{StaticResource IconButton}" Click="Expand_Click" Height="34" HorizontalAlignment="Stretch" ToolTip="Expand widget" automation:AutomationProperties.Name="Expand widget">
          <Path Data="M4,13 L10,7 L16,13" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.6" Width="15" Height="15" Stretch="Uniform" />
        </Button>
      </Grid>
    </Border>
  </Grid>
</Window>
